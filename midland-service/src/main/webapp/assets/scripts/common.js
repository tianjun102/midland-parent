// 保留二级页面链接至本地存储 以便刷新获取历史
/*alert("11");*/
/*$(".savePath").click(function(event) {
	alert("已点击");
	var e = event || window.event;
	if ($(this)[0].href) {
		var path = $(this)[0].getAttribute("href");
		sessionStorage.setItem("href", path);
	} 
})*/
